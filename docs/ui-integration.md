# Product UI Integration Contract

The 课芽 interface is the product shell. Training-day features extend its behavior; they do not create a parallel training-console UI.

## Stable product surfaces

| Product concern | Canonical surface | State responsibility |
| --- | --- | --- |
| Discover prompts and example courses | `/` | Display-only discovery data and links into `/chat` |
| Create a course from a prompt | `/chat` composer | Draft and task creation UI state |
| Explain Agent progress | `/chat` thread | Structured public events, status, errors, retry/cancel affordances |
| Inspect course and page artifacts | `/chat` right learning workspace | Course, page, preview, asset, QA, and export views |
| Browse generated/history items | `/course` | Persisted course/run queries and filters |
| Inspect functional/style templates | `/templates` | Template registry and preview data |

`AiPlayground` and the older course-planner panels remain implementation references. They must not replace `/` or become the visual foundation for new tasks.

## Brand theme

- Product name: `课芽`.
- Primary: sprout green `#397A52`; hover/depth: `#2F6845`.
- Accent: mango yellow `#F2B84B`; canvas: warm cream `#FFF9EE`.
- Product surfaces use the `--keya-*` tokens in `src/app/globals.css`. Legacy internal module and type names are compatibility details, not user-facing brand copy.

## Handbook capability map

- Days 1–6: model calls, tool calls, and Agent events feed the `/chat` controller and public event timeline.
- Days 7–12: `CourseIntent`, `CoursePlan`, professional briefs, and `PageContentDSL` render in the learning workspace.
- Days 13–20: sandboxed HTML previews, assets, multi-page results, typed real-time task progress, and a task/Agent/page observability projection extend the learning workspace; generated HTML stays outside long-lived React UI state when persistence is available.
- Day 13 (implemented): each completed Page DSL established the shared HTML contract, lightweight security preflight, and empty-policy `sandbox` iframe boundary inside the `/chat` learning workspace.
- Day 14 (implemented): `HtmlEngineerAgent` replaces the deterministic demo source with model-generated single-page HTML. Generation state and public validation events stay in `/chat`; the learning workspace provides quick preview, while `/preview/[previewId]` provides a large, 24-hour artifact view backed by a revalidated database record. Neither surface relaxes the Day 13 iframe boundary.
- Day 15 (implemented): a report-only `PageQAAgent` combines deterministic HTML/layout heuristics with semantic six-dimension evaluation. Per-page QA state and public events stay in `/chat`; actionable scores and issues render beside the generated page, while the independent preview receives only an optional schema-validated report from the controller. QA never mutates HTML and does not introduce Repair behavior.
- Day 16 (implemented): `ImagePromptAgent` compiles each Page DSL asset slot into a constrained request for a background, character sticker, icon, or texture. A server-only image Skill validates and stores real raster output behind `/api/assets/[assetId]`; every provider or format failure becomes an explicit CSS/SVG/placeholder fallback, so HTML generation can continue. Page-scoped asset state and public events live in `/chat`. HTML Engineer may reference only approved internal URIs and always keeps text, interaction, and layout as HTML.
- Day 17 (implemented): page asset resolution now reuses a server-only compiled request set for the same Page DSL, VisualBrief, and Image Prompt version before resolving each ready image through a prompt/style/aspect/model content key. This prevents nondeterministic Prompt wording from causing another image-provider call on a same-page repeat. Missing backing files become stale misses, while cache read/write failures and provider fallbacks remain recoverable. Public request-set/image hit, miss, stale, and fallback counts flow through the existing Image Assets event summaries in `/chat`; the learning workspace continues to compose approved background and character assets with real HTML text instead of introducing cache controls or a second UI.
- Day 18 (implemented): `POST /api/courses/generate` is the server-owned serial MVP workflow for one prompt → Intent → Planner → professional briefs → ordered Page Writer/Assets/HTML stages. Its shared `CourseGenerationState` is atomically checkpointed under `.data/courses`, so a failed or cancelled task retains completed pages and resumes without rerunning them. The `/chat` controller maps the batch result into the existing Timeline and page stages; the composer owns cancel intent, while the right learning workspace owns checkpoint recovery and a unified tabbed preview that mounts only the selected sandbox iframe. Page QA remains an optional per-page action, `/course` history remains reserved for Days 34–36, and no new product route or visual system is introduced.
- Day 19 (implemented): `POST /api/courses/tasks` creates a persisted asynchronous run, `GET /api/courses/tasks/[taskId]/events` streams strict `snapshot`/public `event`/`terminal` messages, `PATCH /api/courses/tasks/[taskId]` pauses or resumes one non-terminal task from its persisted checkpoint, and `DELETE /api/courses/tasks/[taskId]` remains the explicit terminal cancellation boundary. The stream uses persisted public-event sequence numbers for SSE `id` and `Last-Event-ID` replay; an in-process EventBus supplies live delivery only after checkpoints are saved. `useSSETask` owns EventSource parsing, validation, reconnect deduplication, and terminal cleanup in the controller data layer. `/chat` maintains an independent task registry, stream bridge, busy state, draft, and attachments for each conversation, so pausing or completing one course cannot overwrite another conversation. It continues to render the existing Composer, appendable Timeline, workspace artifacts, and sandbox previews without a parallel console or framework-native stream chunks.
- Day 20 (implemented): `/chat` enhances the existing Timeline with a pure task/Agent/page read model, separate task and SSE connection states, page counts, derived boundary-event durations, cross-trace recovery markers, and precise Agent/page/error-code failure cards. A typed PageProgressPanel in the right learning workspace compares Page DSL, assets, HTML, and optional QA without making QA a delivery dependency. The inline GenerationLogDrawer renders only the strict public-event allowlist and never serializes raw stream messages, Prompt, DSL/HTML, private event data, or hidden reasoning. These are presentation projections over the Day 19 controller and transport; no `/generate` route, parallel console, or second visual system is introduced.
- Day 21 (implemented): the current fixed multi-Specialist runtime and the future Supervisor architecture are documented separately. This documentation-only delivery does not add routes, UI state, runtime events, Supervisor, Repair, automatic QA, Page Worker isolation, or LangGraph.
- Day 22 (implemented): the server-owned fixed course flow is expressed as an explicit `WorkflowNode[]`. `runCourseGenerationWorkflow` remains the compatibility facade, while `runSequentialWorkflow` checks `requiredInputs`, restricts patches to `produces`, centralizes validated state merges, and reports `WorkflowNodeError.nodeName`; Intent, Planner, Course Design, and page-scoped Writer/Assets/HTML wrappers live in `course-generation-nodes.ts`. This is an internal orchestration refactor: the task API, SSE schemas, controller adapter, Timeline, workspace, sandbox previews, checkpoint cadence, cancellation, and recovery semantics are unchanged, and no new product surface is added.
- Day 23 (implemented): a bounded `SupervisorAgent` receives only a compressed state summary, deterministically computed available nodes, the latest structured failure, and persisted node/page attempt counts. `runSupervisedWorkflow` validates every structured `run`/`retry`/`complete`/`stop` proposal against `requiredInputs`, a three-attempt target budget, cancellation, progress, and a global decision limit before reusing the Day 22 node runner. Each accepted or deterministic stop decision is checkpointed as an allowlisted `supervisor_decision` summary and projected into the existing `/chat` Timeline; SSE, the controller, workspace, routes, and sandbox boundary remain unchanged.
- Day 24 (implemented): the eight active Specialist Prompt pairs now share one versioned library contract with explicit Role, Goal, Inputs, Output Schema, Rules, Forbidden, Examples, and Failure Handling sections. A ninth Repair entry is documentation-only and remains `draft`, with no Agent, Schema, Workflow node, event, route, or UI. Prompt linting and review artifacts stay server/tooling concerns; the existing controller, public SSE projection, Timeline, workspace, and 课芽 visual system are unchanged.
- Day 25 (implemented): each planned page now runs inside an isolated `generatePageWorker` boundary with page-local state, attempts, errors, and events. The worker executes Page Writer → Image Assets → HTML Engineer → existing report-only Page QA. Serial generation respects `dependsOnPageIds`; parallel generation keeps those links as learner-order metadata while independently producing pages through a default concurrency-2 Promise Pool. Worker updates enter `CourseGenerationState` only through a serialized merge/checkpoint queue; the existing SSE controller projects simultaneous page stages and automatic QA into the current Timeline and `PageQualityPanel`. No route or visual system was added.
- Day 26 (implemented): automatic Page QA now preserves the existing six persisted dimension keys while deriving dimension-scoped issue codes and repair hints, applying a server-owned content-first priority order, and optionally adding fixed-viewport Playwright geometry evidence. Screenshot files remain server-only artifacts under `.data`; the shared report exposes only status, an opaque artifact ID, viewport, and metrics. The existing `PageQualityPanel` groups the six handbook dimensions and renders the server issue order; screenshot failure or a missing Chromium binary never blocks the Page Worker, and QA still cannot mutate HTML.
- Day 27 (implemented, quality policy updated 2026-07-24): Page Worker routes a failing `QualityReport` through a deterministic issue classifier and persists each Repair/re-QA result. Content/pedagogy issues may change only located DSL blocks or an explicitly authorized narration field on blockless sparse pages; layout/style/HTML and markup-bound asset issues use uniquely matched replacements or selector-scoped tag-boundary insertions plus the existing HTML/DSL/asset safety contract. Failed execution attempts remain auditable but do not count as completed quality iterations; successful candidates continue until pass, while three consecutive non-improving re-QA results or a separate emergency guard stop safely. `repair_attempt`/`repair_success` summaries extend the existing public event stream, Timeline, and right-workspace `RepairLogPanel`; candidates, diffs, prompts, server paths, and private reasoning never enter UI state.
- Day 28 (implemented): an isolated deterministic StateGraph demo proves the installed LangGraph StateSchema, Node, Edge, Reducer, invoke, and updates-stream APIs without entering the production workflow or frontend.
- Day 29 (implemented): a server-only production-state Graph runner reuses the existing Intent, Planner, Course Design, Page Worker, validation, lifecycle-event, and checkpoint boundaries through a fixed `START → intent → planner → briefs → page workers → finalize → END` topology. The task service still defaults to the handwritten Supervisor workflow and no product route, controller field, or UI control was added. Graph-native chunks never enter the frontend; Day 30 remains responsible for an explicit Graph-stream-to-public-event adapter.
- Day 30 (implemented): new `/chat` tasks explicitly select the persisted `langgraph` runtime source while legacy records continue to parse as `workflow`. The production Graph now consumes `updates/custom`; checkpoint custom chunks pass through a strict server-only mapper that accepts only known graph nodes, validated `CourseGenerationState`, current-trace public events, and contiguous persisted sequences. The task service remains the single persistence/publish owner, so mapped checkpoints enter the existing EventBus and `snapshot`/`event`/`terminal` SSE route without a second generate-graph endpoint or duplicate events. `useSSETask` validates a stable source and the existing Timeline shows only a `Workflow`/`LangGraph` badge; raw chunks, debug data, prompts, private event data, and framework fields never enter browser state.
- Day 31 (implemented, quality policy updated 2026-07-24): the production Graph starts at a rule-first Supervisor and uses schema-validated conditional edges to choose Intent, Planner, Course Design, dependency-aware Page Workers, one Repair/re-QA advancement per graph turn, one-page retry, Finalize, or a deterministic failed terminal. Supervisor decision and recursion guards are sized above the page-level quality loop rather than enforcing a two-repair cost budget; page business rules remain inside Route/Agent/Worker code. Day 30's strict Graph mapper still projects checkpoints into the existing task SSE, controller, `/chat` Timeline, and learning workspace, so no framework-native chunks, new route, duplicate planning logic, or parallel UI was introduced.
- Day 32 (implemented): `/chat` composer accepts at most three txt/md/pdf references and delegates multipart parsing to a typed API client; the presentational composer never calls the business API. Before submission, each attachment card distinguishes parsing, ready, and error states; a ready card shows the grounded summary, an expandable key-fact preview, an explicit next-action prompt, and a reference-aware composer placeholder without exposing raw chunks. A Node-only Route Handler enforces a 5 MB limit, type/header checks, UTF-8 or PDF text extraction, deterministic bounded chunks, and a source-grounded structured summary. Validated Reference Packs persist through the task record and `CourseGenerationState`; Planner selects page-level chunk usages and Page Writer may only use that authorized subset. The existing learning workspace renders summaries, key facts, truncation state, and referenced page names. Public Timeline events still contain summaries only—raw chunks, file paths, prompts, and private reasoning are not event payloads. No upload route, vector database, OCR pipeline, or parallel visual system was added.
- Day 33 (implemented): strict Tool/Skill/Template Cards and bounded registry retrieval let Supervisor and Planner query short capability, template, and reference summaries. Planner keeps the complete functional-template ID/pageType allowlist but receives only related detailed Template Cards and Reference Hits; raw Reference chunks are resolved server-side later from Day 32 pack/chunk IDs. The rule-first Supervisor still derives legal nodes and budgets from validated state, using Skill summaries only as capability context. Existing `/chat` attachments, Timeline, learning-workspace Reference Panel, and `/templates` catalog remain the product surfaces; no route, upload flow, vector database, framework-native stream, or second UI was added.
- Day 34 (implemented): `/chat` keeps prompt/reference entry and adds a compact disclosure for the task API's existing page-count, execution-mode and concurrency fields. Course/task stores expose schema-validated list reads; `GET /api/courses` returns bounded history summaries while `GET /api/courses/[courseId]` returns the durable checkpoint and associated runs. `/course` now owns real search/filter/loading/empty/error states, and `/course/[courseId]` is the justified durable detail URL that reuses the single-mounted sandbox preview. Completed courses can stream a ZIP containing `course.json`, ordered page HTML and `assets/manifest.json` from `GET /api/courses/[courseId]/export`; incomplete courses return to `/chat?course=...` and reuse the existing checkpoint recovery path. No Agent, model, planning rule or parallel visual system was added.
- Day 35 (implemented): the server-side AI client retains finite timeouts and task-owned AbortSignal propagation while adding deterministic `cheap`/`balanced`/`strong` model routing, one-step transient fallback, usage/cache telemetry, and a bounded schema-valid result cache for Intent, Planner, and active Template Card retrieval. Task cancellation remains the existing `DELETE` boundary and now stops image work between slots; an aborted provider call cannot be converted into a visual fallback or trigger later image/cache/HTML work. `/chat` continues to expose only its existing cancel, terminal status, error, retry, and public-event summaries. No model selector, cache control, cost dashboard, route, or parallel UI was added.
- Day 36 (implemented): three fixed five-page Demo cases exercise the existing task API, strict SSE stream, persisted course detail, sandbox previews, QA/Repair evidence, and ZIP export through a local CLI runner. A shared-schema checker reuses the production HTML/DSL/asset contract and enforces semantic outline, six-dimension QA, Playwright evidence, and export baselines. Curated desktop/mobile evidence remains under `docs/demo`; raw model output and archives stay in ignored `.data`. No product route, controller field, Agent rule, or parallel visual system was added.
- Product UI refinement (2026-07-24, section-limit update 2026-07-27): `/chat` now pauses before task creation to maintain a user-facing course brief, asks only for a missing learning goal, and compiles confirmed answers into the existing task prompt. Audience and learning-mode presets are controlled native radios with an explicit selected state. Section count is no longer a blocking UI choice: the backend Intent chooses a sufficient positive section count from content complexity, learning goals, examples, practice, feedback, and summary needs without a fixed maximum; an explicit positive count in natural language remains supported. The thread projects the existing run into one three-stage course status and the right workspace waits for the real outline instead of inventing placeholder sections. Course-content capabilities now use quality-first strong-model routing with balanced fallback, professional-brief output budgets scale with the real outline length, Planner preserves intentional explanation/practice alternation, and course coherence below 85 triggers Repair. Agent, Supervisor, Trace, QA, Repair, execution mode, concurrency, and raw errors no longer render on ordinary product surfaces. `/course/[courseId]` owns the durable learner player with a flat course map, a single learner-mode sandbox iframe, ready-section navigation, local learning position/completion preferences, and optional browser narration controls. The existing task/SSE APIs, checkpoint semantics, generated HTML validation, empty-sandbox boundary, `/preview/[previewId]`, and `/course` history remain unchanged; trusted scripted interaction runtime remains a later capability.
- Course generation reliability refinement (2026-07-24): Page Writer choice drafts now use one nested `questions[]` contract instead of synchronized parallel arrays, with stable question/option IDs materialized server-side. Page-level retries receive bounded deterministic validation feedback from the previous attempt, while terminal retry exhaustion retains a stable root `causeCode`. Task, course-history, Timeline, chat, and learning-workspace projections distinguish cancellation, content-schema failures, timeout, rate limiting, quota, authentication, configuration, and provider failures through allowlisted public copy rather than exposing raw provider or Schema details. Per-tier provider selectors allow quality-first `strong` generation and its `balanced` fallback to resolve to genuinely different configured models while preserving the legacy single-provider behavior when selectors are absent.
- Quality-first Repair refinement (2026-07-24): Page QA/Repair no longer stops after two revisions. Failed or interrupted model calls remain audit records but do not count as completed quality iterations; transient Repair execution retries up to three times, while an unchanged Schema/model contract failure stops after one recovery retry and cancellation, authentication, or configuration errors stop immediately. Each successful candidate is re-QA'd and persists whether the deterministic quality vector improved. Three consecutive non-improving results trigger `QUALITY_STALLED`, with a separate 24-attempt emergency guard preventing implementation loops. LangGraph Supervisor and recursion guards are sized above that page-level safety boundary, and the learner-facing UI describes the process as quality-first iteration rather than exposing a cost budget.
- Generated lesson quality/runtime refinement (2026-07-24): new Page Writer output uses backwards-compatible `PageContentDSL` v2 with a scene kind, code-native visual primitive, bounded motion cue plan, and completion rule. HTML Engineer still emits script-free HTML, now with stable runtime markers; `/course/[courseId]` injects one platform-owned runtime only after contract/security validation and listens through a strict source-checked message protocol. Diagnostic previews keep the empty sandbox, while the learner iframe uses `allow-scripts` without same-origin, forms, popups, downloads, or navigation. Learner position, attempts, answer result, completion and HTML version remain local player state; interaction-required pages gate the next action and degrade to static navigation only after a runtime error. Automatic Page QA now requires screenshots at `922×460`, `712×650`, and `366×500`, exercises choice submission/feedback, and rejects overflow, clipping, leaked feedback, broken interaction, weak first-screen density, or a dominating visual. A 20-case prompt manifest and deterministic baseline/candidate comparison CLI add repeatable visual-quality regression without adding a second product surface.
- Course player presentation refinement (2026-07-27): `/course/[courseId]` keeps the same durable player and trusted single iframe; the desktop course map is collapsed by default to one edge affordance and mobile retains the existing sheet. The HTML canvas fills its bounded learner viewport without iframe scrollbars, carries the small subtitle overlay and fullscreen action, and separates page playback controls from display settings. A collapsible strip centers short thumbnail lists and renders each numbered item as a scaled HTML preview; guided/self-paced modes retain their existing narration and completion behavior. New generated pages carry the platform fluid-canvas marker, and browser QA evaluates the same platform contain-fit runtime that learners receive instead of treating the pre-fit authoring scroll size as a delivery overflow. No route, transport, storage owner, Agent surface, or second visual system was added.
- Conversation management refinement (2026-07-27): `/chat` exposes the persisted conversation title and pinned flag through the existing sidebar, with rename, pin/unpin, and confirmed deletion actions. Active rows display their own generating or paused state, and the composer pauses or resumes only the selected conversation's task. Deleting a conversation first cancels only its non-terminal task, while other course runners, streams, drafts, attachments, and chat content remain untouched.
- Partial-generation recovery refinement (2026-07-27): a failed chapter no longer visually replaces the state of chapters that are still progressing in parallel. While the task remains active, the right learning workspace marks that chapter as temporarily incomplete and lets the batch settle; after terminal failure it offers one bounded retry for all failed chapters from the existing checkpoint, preserving completed pages. A paused task projects checkpoint-level `running` chapters as paused and pending chapters as waiting, without mutating durable page state or leaving active spinners. Explicit checkpoint recovery rearms failed page stages after quota, authentication, or configuration issues are resolved while retaining Repair audit and safety limits.
- Course discovery cover refinement (2026-07-27): the homepage featured row admits only fully completed, exportable course runs; running, paused, failed, cancelled, or incomplete records remain available only in the history gallery and `/course`. Homepage, history gallery, and course-library cards use the completed first lesson as a lazy sandboxed 16:9 HTML cover through a dedicated versioned route, preserving the existing HTML contract, safety preflight, and no-same-origin boundary without serializing generated HTML into list payloads.
- Local generation reliability refinement (2026-07-27): the project has no user-credit, monthly-cost, course-count, or cumulative-token quota. Planner primary and fallback attempts no longer split one timeout budget. Browser QA now measures the delivered fitted lesson canvas, and explicit recovery invalidates only stale pre-fit overflow reports while preserving generated HTML, DSL, assets, and completed lessons. Error checkpoints publish a fresh task snapshot, terminal UI state does not leave sibling lessons spinning, and the concrete provider `causeCode` survives Graph terminalization. Provider-account quota still remains an external prerequisite and cannot be bypassed in application code.

## Frontend boundaries

1. API clients translate HTTP/SSE payloads into shared typed task data.
2. A task controller owns task, course, and page state. Presentational components do not call APIs.
3. `ChatComposer` owns no business logic; it emits user intent and reflects busy/cancel state.
4. `ChatThread` shows conversation content and public execution summaries, not private reasoning.
5. The learning workspace renders structured artifacts and page actions. It should remain transport-agnostic.
6. Route handlers and Agent workflows remain the business source of truth; UI components must not duplicate planning rules.

## UI primitives

- Reusable controls come from the local shadcn/ui source in `src/components/ui`; add only the primitives a product surface needs.
- Product components import icons directly from `lucide-react`. Do not add a second hand-written SVG icon layer.
- The 课芽 tokens and existing product classes remain the visual source of truth. Adopting a primitive must not silently change spacing, color, typography, responsive behavior, or interaction state.
- Keep native semantic elements when a shadcn primitive would change the intended behavior, such as disclosure content or an existing radio group.

When a later task needs a new panel, first place it in one of these surfaces. Add a new product route only when the artifact has its own durable URL and cannot be represented by the existing shell.

The Day 14 preview route remains a temporary full-canvas convenience backed by an expiring database record. Day 34 makes `/course` and `/course/[courseId]` the durable history/detail owners; persisted history never depends on the random preview ID.
