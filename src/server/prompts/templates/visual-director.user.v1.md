请为下面的课程生成 VisualBrief 内容草稿。

CourseIntent：
{{courseIntentJson}}

CoursePlan：
{{coursePlanJson}}

PedagogyPlan：
{{pedagogyPlanJson}}

StoryArc：
{{storyArcJson}}

唯一允许引用的 StyleTemplate：
{{styleTemplateJson}}

pageGuidance 数量必须等于 CoursePlan.pages 数量并保持相同顺序。只返回 JSON object，不输出任何十六进制颜色。
